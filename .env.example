# =========================================
# 프로덕션 (운영)
# =========================================
DATABASE_URL=mysql://user:password@prod-host:3306/security_platform
GUACAMOLE_BASE_URL=https://rdp.example.internal/guacamole
JWT_SECRET=

# =========================================
# 스테이징 (테스트용 — 별도 DB 인스턴스 준비 전까지 로컬 개발 DB 사용)
# 실제 검증 시 아래 값으로 교체해서 사용하고, 절대 프로덕션 값과 함께 켜두지 않는다.
# =========================================
# DATABASE_URL=mysql://user:password@localhost:3306/security_platform_staging
# GUACAMOLE_BASE_URL=https://staging-rdp.example.internal/guacamole
# JWT_SECRET=

# =========================================
# 참고용 (코드 내 하드코딩 금지 — AGENTS.md 참조)
# =========================================
# 1차 대상 사업장 목록
SITES=하남,평동,소촌
