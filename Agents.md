# AGENTS.md — AI 작업 공통 규칙

이 파일은 이 프로젝트에서 작업하는 모든 AI 툴(Claude, Codex, Antigravity 등)이 공통으로 따라야 하는 규칙입니다.
(각 툴이 인식하는 파일명이 달라도 — `CLAUDE.md`, `AGENTS.md` 등 — 내용은 이 파일을 기준으로 동일하게 유지합니다.)

## 코드 스타일 / 구조 원칙
- **메인 파일(서버 진입점, 클라이언트 App 등)은 다른 함수를 "호출"만 하는 역할로 유지한다.** 실제 로직을 메인 파일에 직접 작성하지 않는다.
- 기능 단위로 파일을 분리한다. 기능 하나 = 파일 하나를 기본으로 한다.
- 특정 기능을 수정할 때는 해당 기능이 들어있는 파일을 찾아서 그 파일만 수정한다. 메인 파일을 통째로 다시 쓰지 않는다.
  - (이유: 메인 함수를 계속 고치다가 이전에 만들어둔 기능이 유실되는 문제를 방지하기 위함)

## 절대 하지 말아야 할 것
- 메인 파일/메인 함수에 로직을 직접 재작성해서 기존 구조를 갈아엎지 않는다.
- API 키, DB 접속정보, JWT 시크릿 등은 코드에 직접 쓰지 않는다 — 반드시 `.env`로 관리한다 (`.env.example` 참조).
- 프로덕션 DB(운영 중인 `security_platform` 등)에 직접 테스트하지 않는다 — 스테이징/로컬 DB에서 먼저 검증한다.
- 웹앱/API를 배포할 때 접근권한(전산팀 전용 vs 전체 공개)을 확인하지 않고 배포하지 않는다.

## 이 프로젝트에서 쓰는 용어
- **사업장**: 하남, 평동, 소촌 (1차 대상 3개 사업장)
- **역할 구분**: 서버 운영 = 전산팀 / 에이전트 설치 대상 = 전사 관리직 PC / 대시보드 접근 = 전산팀 전용
- **주요 스키마 용어** (drizzle/schema.ts 기준, 임의로 새 이름 만들지 말 것):
  - `sites` — 사업장
  - `agents` — 설치된 에이전트(PC)
  - `defenderStatus` — Windows Defender 보안 상태
  - `quarantineRecords` — 격리소(위협 탐지) 기록
  - `inspectionCommands` — 원격 점검/스캔 명령
  - `rdpSessions` — RDP 원격 접속 세션
  - `auditLogs` — 감사 로그

## 새 AI 세션 시작 시
작업 시작 전 `SPEC.md`, `상황표.md`를 먼저 읽고 시작한다. 작업 전/후 기록은 `상황표.md`에 남긴다.
