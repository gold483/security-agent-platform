# SPEC.md — Sentinel 보안관제 플랫폼

## 목적
여러 사업장(하남, 평동, 소촌)에 배포된 전사 관리직 PC의 보안 상태를 전산팀 자리에서 원격으로 통합 관제한다.
- 백신(Windows Defender) 상태 점검
- 주요 서비스 실행 상태 점검
- Windows 업데이트 권장/현황 파악
- 여러 사업장을 직접 돌아다니지 않고, 서버에서 클라이언트(에이전트)로 동작 명령을 내려 정보 수집 및 조치 수행

## 사용자 (역할별 접근 범위)
| 구성요소 | 사용 주체 | 비고 |
|---|---|---|
| 중앙 서버(관제 서버) | 전산팀 | 배포·운영 주체 |
| 에이전트 클라이언트 | 전사 관리직 PC | 각 사업장(하남/평동/소촌)에 설치, 서버 명령 수신 및 정보 보고 |
| 웹 대시보드 | 전산팀 전용 | 관리직 등 타 부서는 접근 불가 |

## 입력/출력 데이터
- 에이전트 → 서버: 하트비트, Defender 상태(보호 활성화 여부, 백신 상태, 서명 버전), 서비스 실행 상태, 격리소(Quarantine) 기록, Windows 업데이트 현황
- 서버 → 에이전트: 원격 명령(Full Scan, Quick Scan, 서명 업데이트, Windows 업데이트 권장 알림)
- 저장소: MySQL/TiDB (sites, agents, defenderStatus, quarantineRecords, inspectionCommands, rdpSessions, auditLogs)
- RDP 게이트웨이: Apache Guacamole 연동, 1회성 접속 토큰

## 제약사항
- **네트워크**: 사내망(intranet) 내에서만 서버·대시보드 접속 가능해야 함 — 외부 인터넷 직접 노출 금지
- **접근권한**: 대시보드는 전산팀 계정만 접근. 에이전트는 관리직 PC에 배포되지만 대시보드 열람 권한 없음
- **사업장 범위**: 1차 대상은 하남, 평동, 소촌 3개 사업장
- **보안**: API 키/DB 자격증명 등은 반드시 `.env`로 관리 (AGENTS.md 참조)

## 완료 기준
1차 완료(파일럿) 기준: 하남 사업장에 에이전트를 배포하여
- 보안 상태(Defender/서비스) 점검이 정상 수집되고
- 서버에서 내린 원격 명령(스캔 등)이 정상 동작하며
- 대시보드에서 전산팀이 해당 정보를 확인할 수 있으면 1차 완료로 본다.

이후 평동, 소촌으로 순차 확대.
